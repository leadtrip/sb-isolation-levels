spring:
  liquibase:
    enabled: false
  flyway:
    enabled: true
    locations: classpath:db/migration
  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        cache:
          use_second_level_cache: false
          region.factory_class: org.hibernate.cache.internal.NoOpRegionFactory